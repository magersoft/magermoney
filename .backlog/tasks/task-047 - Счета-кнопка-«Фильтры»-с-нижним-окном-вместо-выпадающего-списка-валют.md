---
id: TASK-047
title: 'Счета: кнопка «Фильтры» с нижним окном вместо выпадающего списка валют'
status: Done
assignee:
  - '@claude'
created_date: '2026-09-20 07:35'
updated_date: '2026-09-23 07:58'
labels: []
dependencies:
  - TASK-050
references:
  - packages/ui/src/components/currency-select/CurrencySelect.vue
  - apps/web/src/modules/currencies/ui/AppCurrencySelect.vue
  - packages/ui/src/components/country-select/CountrySelect.vue
type: feature
ordinal: 45000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Сейчас на экране «Счета» фильтр один: выпадающий список «Все валюты». Отфильтровать счета по стране, типу (карта или счёт), типу карты, сроку действия или сумме нельзя. Вместо одного списка нужна аккуратная кнопка «Фильтры», которая открывает снизу окно (dialog / content modal) со всеми настройками сразу.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Вместо выпадающего списка «Все валюты» на экране «Счета» одна аккуратная кнопка «Фильтры»; когда фильтры активны, на ней видно их число
- [x] #2 Нажатие открывает нижнее окно (dialog content modal) с настройками: валюта, страна, карта или счёт, тип карты, срок действия, сумма от и до
- [x] #3 Валюта и страна выбираются автокомплитом с поиском и мультивыбором; выбранные значения видны как чипы и снимаются по одному
- [x] #4 Есть кнопки «Применить» и «Сбросить»; список счетов и итоговая сумма пересчитываются по применённым фильтрам
- [x] #5 Пустой результат показывает понятное состояние с быстрым сбросом фильтров
- [x] #6 Окно доступно с клавиатуры и экранных читалок (фокус, Esc, подписи), закрывается свайпом вниз и корректно работает в светлой и тёмной теме
- [x] #7 Поля «тип карты» и «срок действия» скрыты или неактивны, пока данных для них нет в счетах, и это указано в задаче-зависимости
- [x] #8 Тесты покрывают применение, сброс и мультивыбор
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Решения (согласованы с владельцем 23.09): мультивыбор — режим multiple в CurrencySelect и CountrySelect; свайп вниз — общий жест для всех нижних окон приложения (SheetContent side=bottom и QuickActionSheet).
1. packages/ui: чистая функция swipeOutcome + композабл useSwipeDismiss; подключить в SheetContent (side=bottom) и QuickActionSheet, с ручкой-полоской; уважать prefers-reduced-motion и прокрутку содержимого.
2. packages/ui: режим multiple в CurrencySelect и CountrySelect — model string[], список не закрывается, выбранное отмечено, чипы под полем снимаются по одному; тесты.
3. apps/web accounts/domain: чистый фильтр счетов (валюты, страны, виды, тип карты, срок действия, сумма от/до в валюте отображения) + число активных фильтров + доступные варианты; тесты.
4. AccountsFilterSheet: черновик, Применить/Сбросить; поля типа карты и срока скрыты, если в счетах нет таких данных.
5. AccountsPage: кнопка «Фильтры» с числом, чипы применённых фильтров, итог и счётчик по отфильтрованным счетам, пустой результат с быстрым сбросом.
6. Тексты ru/en, тесты страницы (применение, сброс, мультивыбор), lint/typecheck/test/build, impeccable-аудит, проверка в браузере в светлой и тёмной теме.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Решения: мультивыбор — режим multiple в CurrencySelect/CountrySelect (чипы под полем, галочка в списке, список не закрывается); свайп вниз — общий жест для всех нижних окон (SheetContent side=bottom и QuickActionSheet) по решению владельца, с ручкой-полоской. Обработчики жеста висят в шаблоне (v-on): template ref на DialogContent reka отдаёт $el не сразу и нереактивно, в jsdom это не видно, в Chrome жест был мёртв.
AC #7: данные для типа карты и срока действия уже есть (cardType, cardExpires в контракте), поэтому задача-зависимость не нужна; поля скрыты, пока ни одна карта их не указывает (filterChoices).
Сумма от/до сравнивается в валюте отображения; счёт без курса при заданной сумме не проходит. «Скоро истекает» = до 90 дней, карта действует до последнего дня указанного месяца.
Проверка: bun run test (ui 344, web 399, api 256, domain 116, contracts 41), lint, typecheck, build — зелёные; мутационные проверки фильтра и страницы; в Chrome на 375px в светлой и тёмной теме: мультивыбор, применение, пересчёт итога (1819,05 = 2306,25 − 487,20), пустое состояние, свайп (длинный закрывает, короткий возвращает), Escape и возврат фокуса на кнопку.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: claude
created: 2026-09-20 17:15
---
Обновление контекста (20.09.2026): выпадающий список «Все валюты» на экране «Счета» — последний нативный `<select>` в приложении, остальные места уже переведены на общий компонент выбора валюты из TASK-050.

Что есть готового к моменту старта задачи:

- `packages/ui/src/components/currency-select/CurrencySelect.vue` — автокомплит с поиском по названию (ru/en), коду и символу, флагами, группами «фиат / крипта» и блоком «частые» сверху (`frequent`). Два варианта: `row` (поле формы) и `compact` (узкий триггер рядом с суммой). Есть `disabledCodes` + `disabledLabel`, `hint`, `error`. Ранжирование и группировка — чистые функции в `filter.ts`.
- `apps/web/src/modules/currencies/ui/AppCurrencySelect.vue` — обёртка, которая подставляет подключённые валюты и все строки перевода. Для фильтра, скорее всего, нужен именно он, но со списком валют самих счетов, а не всех подключённых.
- `packages/ui/src/components/country-select/CountrySelect.vue` — то же самое для страны (AC #3), уже с флагами и поиском.
- `apps/web/test/fixtures/currency-picker.ts` — хелпер `pickCurrency(picker, code)` для тестов (AC #8).

Чего не хватает под AC #3: оба компонента одиночного выбора — `v-model` это одна строка. Для мультивыбора и чипов нужно либо добавить в `CurrencySelect` режим `multiple` (reka-ui Combobox это умеет), либо собрать чипы вокруг него в самом окне фильтров. Решение стоит принять до начала работы: первый путь дороже, но избавляет от второго похожего контрола в дизайн-системе.

Ещё одно: список валют для фильтра — это валюты счетов человека плюс «Все», а не каталог из 234 позиций; группы «фиат / крипта» на такой короткий список, скорее всего, лишние.
---
<!-- COMMENTS:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Экран «Счета»: вместо выпадающего списка валют одна кнопка «Фильтры» с числом активных фильтров. Нижнее окно фильтрует по валюте и стране (мультивыбор с чипами), виду счёта, типу карты, сроку действия и сумме от/до; изменения попадают в черновик, «Применить» и «Сбросить» его фиксируют. Список, счётчик и итог пересчитываются, каждый фильтр снимается своим чипом, для пустого результата есть сброс в одно касание. В дизайн-системе: режим multiple у CurrencySelect/CountrySelect и закрытие свайпом вниз у всех нижних окон. Проверено тестами (включая мутационные), lint/typecheck/build и вручную в Chrome в обеих темах.
<!-- SECTION:FINAL_SUMMARY:END -->
