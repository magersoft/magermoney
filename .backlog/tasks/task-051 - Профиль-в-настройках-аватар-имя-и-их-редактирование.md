---
id: TASK-051
title: 'Профиль в настройках: аватар, имя и их редактирование'
status: In Progress
assignee:
  - '@claude'
created_date: '2026-09-20 10:39'
updated_date: '2026-09-23 08:48'
labels: []
dependencies: []
ordinal: 49000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Имя (displayName) сейчас редактируется на SettingsPage в общем столбце строк, между языком и темой, — человек не видит, где здесь «он сам», а где параметры приложения. Аватар (ProfileAvatar) рисует только инициал имени и нигде не настраивается: в шапке и в приветствии на главной он одинаковый у всех и молчит о том, что его вообще можно поменять.

Нужен блок «Профиль» первой секцией «Настроек»: крупный аватар, имя и e-mail рядом с ним, редактирование имени и выбор аватара здесь же. Фото не храним (нет файлового хранилища и не хочется его заводить ради одного изображения) — аватар задаётся выбором эмодзи и цвета диска, они едут сквозь всю вертикаль: колонки в profiles, поля в контрактах, PATCH /me, доменное правило отображения и сам компонент ProfileAvatar, который читает их вместо одного инициала. E-mail только показываем: он приходит из сессии Supabase и здесь не меняется.

Открытые вопросы для исполнителя, решение зафиксировать в плане: откуда берётся набор цветов диска (токены палитры против отдельного списка) и как ограничен набор эмодзи (свободный ввод одного символа против готовой сетки). Инициал остаётся дефолтом, когда эмодзи не выбран, — существующее поведение ProfileAvatar не должно потеряться.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 В profiles есть колонки эмодзи и цвета аватара, обе необязательные, миграция применяется на чистой базе и на существующих данных
- [ ] #2 Поля аватара есть в ProfileDtoSchema и в UpdateProfileInputSchema, где их отсутствие не затирает сохранённое значение, а недопустимое значение цвета или длины эмодзи отклоняется
- [ ] #3 GET /me возвращает поля аватара, PATCH /me их меняет; use case фильтрует по userId
- [ ] #4 ProfileAvatar показывает выбранный эмодзи на выбранном цвете, при отсутствии эмодзи — инициал имени, при отсутствии имени — прежнюю силуэтную иконку; во всех трёх случаях контраст текста к фону проходит по обеим темам
- [ ] #5 Первая секция SettingsPage — «Профиль»: крупный аватар, имя, e-mail из сессии; имя редактируется и сохраняется без отдельной кнопки Save, как сейчас
- [ ] #6 Выбор эмодзи и цвета доступен с клавиатуры и с озвучиванием выбранного варианта скринридером
- [ ] #7 Изменённый аватар сразу виден в шапке и в приветствии на главной, без перезагрузки страницы
- [ ] #8 Строка редактирования имени больше не дублируется ниже, среди параметров приложения
- [ ] #9 Строки локализации добавлены в en.json и ru.json
- [ ] #10 Тесты покрывают: контракты (частичное обновление и отклонение недопустимого значения), PATCH /me, доменный выбор между эмодзи, инициалом и силуэтом, и отрисовку секции профиля на SettingsPage
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Решения по открытым вопросам:
- Цвета диска: те же 8 имён, что у карточек счетов (ACCOUNT_COLORWAYS из packages/domain). У каждой заливки в ACCOUNT_COLORWAY_FILLS уже есть подобранные чернила, и контраст доказан в tokens-contrast.test.ts; заливки одинаковы в светлой и тёмной теме, так что одно доказательство покрывает обе. Отдельный список означал бы второе доказательство контраста. null = прежний диск bg-surface/text-ink.
- Эмодзи: в UI готовая сетка (радиогруппа), в контракте — любой один графемный кластер эмодзи (Intl.Segmenter + \p{Extended_Pictographic}/Regional_Indicator), не длиннее 16 кодпоинтов; в БД check char_length 1..16. Сетку можно менять без миграции. null = инициал.

Шаги:
1. Миграция: profiles.avatar_emoji text check (1..16), avatar_color text check (in colorway names), обе nullable.
2. packages/domain: AVATAR_COLORS (= ACCOUNT_COLORWAYS) + isAvatarEmoji (тест, 100% покрытие).
3. contracts: avatarEmoji/avatarColor в ProfileDtoSchema (nullable) и UpdateProfileInputSchema (nullable().optional()); тесты частичного обновления и отказа.
4. API: Profile/ProfilePatch, pg/memory репозитории, тест PATCH /me (установка, сброс null, отсутствие поля не затирает, 400 на неверном цвете/эмодзи).
5. web domain: avatarFace(profile, email) → emoji | initial | silhouette; тест.
6. ProfileAvatar читает avatarFace и цвет (cardFill чернила).
7. SettingsPage: первая секция «Профиль» — крупный аватар, имя (сохранение на change/blur, как было), e-mail из сессии; кнопка «Изменить аватар» открывает Sheet с радиогруппами эмодзи и цвета (roving tabindex, aria-checked), каждый выбор сохраняется сразу через тот же оптимистичный PATCH — шапка и приветствие обновляются из общего кэша ['me']. Строка имени убрана из параметров.
8. i18n en/ru, тесты SettingsPage, lint/typecheck/test.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Реализовано в 7f9497a (миграция→домен→контракты→API) и fa61270 (Settings, ProfileAvatar, AvatarSheet).
- Миграция проверена: с нуля на отдельной scratch-БД (все миграции + заглушка auth) и поверх 36 существующих профилей в транзакции; check-ограничения отклоняют 'magenta', '' и 17 символов. migration-reviewer: блокеров нет.
- Локальная БД: история миграций рассинхронизирована (000020–000022 применены, но не записаны), поэтому 000023 применена через psql напрямую; supabase db reset не запускался, чтобы не стереть локальные данные.
- Попутно: записи профиля сериализованы (scope 'profile' в TanStack), иначе быстрые выборы стрелками могли лечь на сервер в обратном порядке.
- Добавлена проверка контраста голой заливки диска (карточный тест мерит только через блик); мутации (янтарь с белыми чернилами) ловятся.
- Все тесты/линт/typecheck зелёные. Не проверено в браузере: dev-сервер требует входа.
<!-- SECTION:NOTES:END -->
